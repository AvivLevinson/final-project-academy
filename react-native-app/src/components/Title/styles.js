import {StyleSheet} from 'react-native';


const styles = StyleSheet.create({
    text: {
        color: '#4756DF',
        borderBottomWidth: 1,
        fontSize: 20,
        marginTop: 10,
        fontWeight:'bold',
      }
});

export default styles;

